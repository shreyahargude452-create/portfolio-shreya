<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shreya Shivaji Hargude | Software Developer Portfolio</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;600&family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* CSS RESET & VARIABLES */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        :root {
            --bg-color: #0b0f19;
            --card-bg: #111827;
            --text-main: #f3f4f6;
            --text-muted: #9ca3af;
            --accent-color: #38bdf8;
            --accent-glow: rgba(56, 189, 248, 0.2);
            --border-color: #1f2937;
            --font-sans: 'Inter', sans-serif;
            --font-mono: 'Fira Code', monospace;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: var(--font-sans);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* NAVIGATION */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(11, 15, 25, 0.85);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border-color);
            z-index: 1000;
        }

        nav {
            max-width: 1100px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.2rem 2rem;
        }

        .logo {
            font-family: var(--font-mono);
            font-weight: 600;
            font-size: 1.1rem;
            color: var(--accent-color);
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.95rem;
            transition: color 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--accent-color);
        }

        /* LAYOUT CONTAINERS */
        section {
            padding: 6rem 2rem;
            max-width: 1100px;
            margin: 0 auto;
        }

        .section-title {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 2.5rem;
            position: relative;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: -8px;
            width: 50%;
            height: 3px;
            background-color: var(--accent-color);
            border-radius: 2px;
        }

        /* HERO SECTION */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            padding-top: 5rem;
        }

        .hero-subtitle {
            font-family: var(--font-mono);
            color: var(--accent-color);
            font-size: 1rem;
            margin-bottom: 1rem;
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: 700;
            line-height: 1.2;
            margin-bottom: 1rem;
        }

        .hero-description {
            font-size: 1.2rem;
            color: var(--text-muted);
            max-width: 600px;
            margin-bottom: 2rem;
        }

        .btn {
            display: inline-block;
            padding: 0.8rem 1.8rem;
            background-color: transparent;
            color: var(--accent-color);
            border: 1px solid var(--accent-color);
            border-radius: 6px;
            text-decoration: none;
            font-family: var(--font-mono);
            font-size: 0.95rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn:hover {
            background-color: var(--accent-glow);
            transform: translateY(-2px);
        }

        /* GRID CARDS */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .card {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1.8rem;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease, transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .card.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .card.visible:hover {
            transform: translateY(-6px);
            border-color: var(--accent-color);
            box-shadow: 0 10px 20px -10px var(--accent-glow);
        }

        /* ABOUT & EDUCATION */
        .education-card {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .edu-degree {
            font-size: 1.2rem;
            font-weight: 600;
        }

        .edu-institution {
            color: var(--accent-color);
            font-size: 1rem;
        }

        .edu-details {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        /* SKILLS */
        .skill-tag {
            display: inline-block;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--border-color);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-family: var(--font-mono);
            font-size: 0.9rem;
            margin: 0.3rem;
            color: var(--text-main);
            transition: all 0.3s ease;
        }

        .skill-tag:hover {
            border-color: var(--accent-color);
            color: var(--accent-color);
            transform: translateY(-2px);
        }

        /* CERTIFICATIONS */
        .cert-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .cert-issuer {
            color: var(--text-muted);
            font-size: 0.95rem;
        }

        /* CONTACT FORM */
        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
            max-width: 600px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .form-group label {
            font-family: var(--font-mono);
            font-size: 0.85rem;
            color: var(--text-muted);
        }

        .form-group input,
        .form-group textarea {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 0.8rem;
            border-radius: 6px;
            color: var(--text-main);
            font-family: var(--font-sans);
            font-size: 1rem;
            outline: none;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            border-color: var(--accent-color);
        }

        /* FOOTER */
        footer {
            text-align: center;
            padding: 2rem;
            border-top: 1px solid var(--border-color);
            color: var(--text-muted);
            font-size: 0.9rem;
            font-family: var(--font-mono);
        }

        /* RESPONSIVE DESIGN */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }

            .nav-links {
                display: none;
                flex-direction: column;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: var(--card-bg);
                padding: 1.5rem;
                border-bottom: 1px solid var(--border-color);
            }

            .nav-links.active {
                display: flex;
            }

            .menu-toggle {
                display: block;
                cursor: pointer;
                color: var(--text-main);
                font-size: 1.5rem;
            }
        }

        .menu-toggle {
            display: none;
        }
    </style>
</head>
<body>

    <!-- NAVIGATION -->
    <header>
        <nav>
            <a href="#" class="logo">&lt;Shreya Hargude /&gt;</a>
            <div class="menu-toggle" id="mobile-menu">&#9776;</div>
            <ul class="nav-links" id="nav-list">
                <li><a href="#about">About</a></li>
                <li><a href="#skills">Skills</a></li>
                <li><a href="#certifications">Certifications</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <!-- HERO SECTION -->
        <section class="hero">
            <span class="hero-subtitle">Hello, my name is</span>
            <h1 class="hero-title">Shreya Shivaji Hargude.</h1>
            <p class="hero-description">
                I build functional software solutions. I am a Computer Science student dedicated to writing clean code and developing practical applications.
            </p>
            <a href="#contact" class="btn">Get In Touch</a>
        </section>

        <!-- ABOUT & EDUCATION SECTION -->
        <section id="about">
            <h2 class="section-title">About & Education</h2>
            <div class="grid">
                <div class="card education-card">
                    <span class="edu-institution">JSPM College, Wagholi</span>
                    <h3 class="edu-degree">B.Sc. in Computer Science (BCS)</h3>
                    <p class="edu-details">Currently in Second Year</p>
                    <p style="margin-top: 1rem; color: var(--text-muted);">
                        Focusing on core programming fundamentals, object-oriented design, hardware architectures, and relational database management.
                    </p>
                </div>
                <div class="card">
                    <h3 style="margin-bottom: 0.8rem; font-size: 1.2rem;">Engineering Philosophy</h3>
                    <p style="color: var(--text-muted);">
                        I focus on logical problem-solving and software efficiency. My goal is to transform theoretical computer science concepts into structured, functional software applications.
                    </p>
                </div>
            </div>
        </section>

        <!-- SKILLS SECTION -->
        <section id="skills">
            <h2 class="section-title">Technical Skills</h2>
            <div style="margin-top: 1rem;">
                <span class="skill-tag">C</span>
                <span class="skill-tag">C++</span>
                <span class="skill-tag">Python</span>
                <span class="skill-tag">Advanced Java</span>
                <span class="skill-tag">MySQL</span>
                <span class="skill-tag">Relational Databases</span>
                <span class="skill-tag">Object-Oriented Programming (OOP)</span>
            </div>
        </section>

        <!-- CERTIFICATIONS SECTION -->
        <section id="certifications">
            <h2 class="section-title">Certifications</h2>
            <div class="grid">
                <div class="card">
                    <h3 class="cert-title">Certificate of Completion in Python Programming</h3>
                    <p class="cert-issuer">Demonstrated proficiency in Python core structures, algorithms, and modular program development.</p>
                </div>
                <div class="card">
                    <h3 class="cert-title">Coursework Accomplishments</h3>
                    <p class="cert-issuer">Completed specialized practical modules in C, C++, Advanced Java, and MySQL Database Administration.</p>
                </div>
            </div>
        </section>

        <!-- CONTACT SECTION -->
        <section id="contact">
            <h2 class="section-title">Get In Touch</h2>
            <p style="color: var(--text-muted); margin-bottom: 2rem; max-width: 500px;">
                Whether you have a question, a potential project, or just want to connect, feel free to send a message below.
            </p>
            <form class="contact-form" id="portfolio-form" action="https://formspree.io/f/your_form_id" method="POST">
                <div class="form-group">
                    <label for="name">NAME</label>
                    <input type="text" id="name" name="name" required placeholder="Your Name">
                </div>
                <div class="form-group">
                    <label for="email">EMAIL</label>
                    <input type="email" id="email" name="email" required placeholder="your.email@example.com">
                </div>
                <div class="form-group">
                    <label for="message">MESSAGE</label>
                    <textarea id="message" name="message" rows="5" required placeholder="Hello Shreya, I'd like to connect..."></textarea>
                </div>
                <button type="submit" class="btn" style="align-self: flex-start;">Send Message</button>
            </form>
        </section>
    </main>

    <!-- FOOTER -->
    <footer>
        <p>Designed & Developed by Shreya Shivaji Hargude</p>
    </footer>

    <!-- JAVASCRIPT -->
    <script>
        // Mobile Navigation Toggle
        const menuToggle = document.getElementById('mobile-menu');
        const navList = document.getElementById('nav-list');

        menuToggle.addEventListener('click', () => {
            navList.classList.toggle('active');
        });

        // Form submission handling logic
        document.getElementById('portfolio-form').addEventListener('submit', function(e) {
            // Note: Remove e.preventDefault() if relying strictly on standard Formspree page redirect
            const name = document.getElementById('name').value;
            alert(`Thank you, ${name}! Your message has been sent successfully.`);
        });

        // Smooth scroll animation using observer class toggle
        const cards = document.querySelectorAll('.card');
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if(entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });

        cards.forEach(card => observer.observe(card));
    </script>
</body>
</html>